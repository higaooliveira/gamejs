{
  "name": "js",
  "version": "1.0.0",
  "description": "",
  "babel":{
      "presets":["env"]
  },
  "main": "gamebabel.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel": "babel gamebabel.js --watch --out-file game.js"
  },
  "author": "Higor",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
